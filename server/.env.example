PORT=2567
# lock to your web origin in production, e.g. https://play.yourgame.app
CORS_ORIGIN=*
# optional — enables the AI validation layer (server-side only, never in the client)
ANTHROPIC_API_KEY=
AI_VALIDATOR_MODEL=claude-haiku-4-5-20251001
# optional — enables durable match persistence (service key: server-side ONLY)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
